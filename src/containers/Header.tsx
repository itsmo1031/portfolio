const Header = () => {
  return (
    <header className="animate-gradient from-primary via-secondary-500 to-primary sticky top-0 z-50 h-[10px] w-full bg-gradient-to-r bg-[length:200%_100%]" />
  );
};

export default Header;
